#-----------------------------
# Path
#-----------------------------

# Todo: Add path inclusions here

#-----------------------------
# linuxbrew
#-----------------------------
[ -d /home/linuxbrew ] && eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"

#-----------------------------
# pyenv
#-----------------------------
export PATH="$HOME/.pyenv/bin:$PATH"
[ -d $HOME/.pyenv ] && eval "$(pyenv init --path)"
[ -d $HOME/.pyenv ] && eval "$(pyenv virtualenv-init -)"

